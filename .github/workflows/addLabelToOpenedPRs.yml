name: Add ReviewMe Labels

on:
  pull_request:
    branches: 
      - master

jobs:
  add_labels:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/labeler@v2
        with:
          repo-token: "${{secrets.GITHUB_TOKEN}}"
          configuration-path: .github/MultiLabel.yml
